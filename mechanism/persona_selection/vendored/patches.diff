diff --git a/eval/eval_persona.py b/eval/eval_persona.py
index 61ac5b6..02e4a0b 100644
--- a/eval/eval_persona.py
+++ b/eval/eval_persona.py
@@ -46,9 +46,13 @@ def sample_steering(model, tokenizer, conversations,  vector, layer, coef, bs=20
         batch = prompts[i:i+bs]
         tokenized_batch = tokenizer(batch, return_tensors="pt", padding=True)
         tokenized_batch = {k: v.to(model.device) for k, v in tokenized_batch.items()}
+        eos_ids = [tokenizer.eos_token_id]
+        im_end_id = tokenizer.convert_tokens_to_ids("<|im_end|>")
+        if im_end_id is not None and im_end_id >= 0:
+            eos_ids.append(im_end_id)
         with ActivationSteerer(model, vector, coeff=coef, layer_idx=layer-1, positions=steering_type):
             with torch.no_grad():
-                output = model.generate(**tokenized_batch, do_sample=(temperature > 0), temperature=temperature, top_p=top_p, max_new_tokens=max_tokens,use_cache=True, min_new_tokens=min_tokens)
+                output = model.generate(**tokenized_batch, do_sample=(temperature > 0), temperature=temperature, top_p=top_p, max_new_tokens=max_tokens,use_cache=True, min_new_tokens=min_tokens, eos_token_id=eos_ids)
         prompt_len = tokenized_batch["input_ids"].shape[1]
         output = [tokenizer.decode(o[prompt_len:], skip_special_tokens=True) for o in output]
         outputs.extend(output)
@@ -61,7 +65,9 @@ def sample(model, tokenizer, conversations, top_p=1, max_tokens=1000, temperatur
         top_p=top_p,
         max_tokens=max_tokens,
         skip_special_tokens=True,
-        stop=[tokenizer.eos_token],
+        # Qwen2 base tokenizer's eos is <|endoftext|>; ChatML turns end with
+        # <|im_end|>, which must also terminate generation.
+        stop=[tokenizer.eos_token, "<|im_end|>"],
         min_tokens=min_tokens
     )
 
diff --git a/judge.py b/judge.py
index 47da905..9142bbe 100644
--- a/judge.py
+++ b/judge.py
@@ -1,3 +1,4 @@
+import asyncio
 import os
 from typing import Dict, List
 import math
@@ -48,15 +49,24 @@ class OpenAiJudge:
 
     async def logprob_probs(self, messages) -> dict:
         """Simple logprobs request. Returns probabilities. Always samples 1 token."""
-        completion = await openai.chat.completions.create(
-            model=self.model,
-            messages=messages,
-            max_tokens=1,
-            temperature=0,
-            logprobs=True,
-            top_logprobs=20,
-            seed=0
-        )
+        last_exc = None
+        for attempt in range(6):
+            try:
+                completion = await openai.chat.completions.create(
+                    model=self.model,
+                    messages=messages,
+                    max_tokens=1,
+                    temperature=0,
+                    logprobs=True,
+                    top_logprobs=20,
+                    seed=0
+                )
+                break
+            except Exception as e:  # OpenRouter can 429/5xx under judge concurrency
+                last_exc = e
+                await asyncio.sleep(2 ** attempt)
+        else:
+            raise last_exc
         try:
             logprobs = completion.choices[0].logprobs.content[0].top_logprobs
         except IndexError:
